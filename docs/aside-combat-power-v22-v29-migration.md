# v22アサイド・v29戦闘力のローカル移行（2026-09-26）

状態：利用者保存済みxlsxの79使徒を照合し、通常生成を1回実施。計算fixture・管理画面／使徒データの隔離ブラウザ焦点検証済み。既存テスト残件は下記。commit／push／公開前。

## 正本と入力

- アサイドは `Analyze/Trickcal_v22_aside_tiers` の AsideLevel/AsideGrade 原JSONと解析を参照する。キャロットのAside HP 2055/204、魔攻216/30、両防御324/45、ウイHP 4680/465は現行xlsxのBase相当値・Growth値と一致する。本体Tierは代用しない。
- 標準比較対象の原AsideGrade 93 Hero ID × A1/A2/A3 = 279行で、HP・主攻撃・両防御の倍率がそれぞれ1/1.03/1.06と一致した。その他の補助・特殊ID、A4/A5、限界突破には一律に拡張しない。
- 戦闘力の低学年・高学年・パッシブ係数は `Analyze/Trickcal_v29_combat_power/sources/sep21/HeroInfo.json` の索引34/35/36から取り、[貼付候補TSV](migration/combat-power-coefficients-v29.tsv)へ使徒ID、参照Hero UID、出典を記載した。生成スクリプトは索引34/35/36/37/41について原JSONとの照合も行う。アサイド係数は利用者の2026-09-26指定により全使徒0.7を採用し、原HeroInfoの索引41と異なる場合があるため、その値は比較用の別列に残す。A2未満では係数を適用しない。
- ダーヤは同名・同model名の9原UIDから自動選択できない。利用者が2026-09-26に原Hero UID `10001` を指定したため、そのIDに限り明示対応した。現行xlsx 79使徒の候補TSVは79件で、[ID要確認一覧](migration/combat-power-unresolved-v29.tsv)は0件。名前一致だけで複数原レコードを結合しない。
- Renewaのシート上の参照Hero UIDはゲーム内のAwaken実体を表すとは限らない、との利用者注記がある。UID一致のみをゲーム内再現の証拠とはしない。
- `使徒基礎設定`には既存の`攻撃速度基礎`と`戦闘力補正値`に加えて、TSVの4係数列を入力する。2026-09-26保存版xlsxで79行すべてTSVの採用係数と一致し、攻速・補正値の欠落もない。旧Aは攻速のaliasにしない。旧Bは新補正値と同義の場合だけ移行でき、値が矛盾すれば生成を拒否する。欠落は0へ補完しない。
- `アサイドTier設定`の `HP`／`物理攻撃力`／`魔法攻撃力`／`物理防御力`／`魔法防御力` 各列について、`_AsideBonus`を`_AsideBase`へ改名し、`_AsideGrowth`と`_AsideTier`を維持、`_AsideStarBonus`を削除する。移行期間の旧Bonusは同義Baseとしてだけ受け付ける。両方に異なる値があれば拒否する。補助計算列の整理は利用者が必要性を判断する。

## 計算境界

- アサイド寄与は、各原BaseとGrowthに対し `(Base + Growth × (Lv−1)) × 段階倍率`。BaseとGrowthの表示内訳は別に保持し、最終ステータスは内訳の表示丸めを経由しない。未解放は寄与0、解放済みで必要値が欠落した場合は未登録として区別する。従来のStarBonus加算・途中floor・本体Tierへのfallbackは使用しない。アサイド全体効果・特殊効果の経路は残す。
- 戦闘力は内部ステータスを使い、主攻撃×2.1、防御合算×0.7、HP×0.08、会心4項×各0.7、攻速×0.6を、この順で各積だけ3桁AwayFromZeroに丸めて加算。外側は `1 + 低Lv×係数 + 高Lv×係数 + パッシブLv×係数 + (A2以上の公開済みアサイド係数) + 戦闘力補正値`。最後にTruncate。学年1の現条件を維持する。
- 内部攻速には現段階で `攻撃速度基礎` を使う。非戦闘時の追加攻速補正が必要かは未確定。DPSの速度計算へこの列を接続しない。表示整数や旧保存snapshotから内部小数を逆算しない。保存された旧戦闘力は再計算対象の派生値として無効化する。
- 通常の本体・装備・全体補正のfloorは今回撤去しない。v29の集計式を実装したことは、全アカウント・全版のゲーム内戦闘力の完全再現を意味しない。表示整数で評価したポーシャー例は参照式で117709、原記録117714であり、完全一致の期待値に使わない。

## 生成・検証と残件

- 利用者から保存・Excel終了・別生成なしの確認を受け、xlsx SHA-256 `23CCA3D09B35BD4143E2F488926EBF3E335DF602B7332CAED1075639A0CB627D` をrepo外へ保全した。`tools\generate-all.bat`を2026-09-26に1回実行し14工程完了。生成後もxlsxはバックアップと同hash。生成物の編集前バックアップと復元元は `D:/Games/etc/trickcal/backups/aside-combat-v22-v29-20260926-013353/MANIFEST.tsv`、標準生成自身のバックアップは `backups/generated/20260926_015527_370_18156`。
- 生成差分ログは `tmp/generate-change-20260926_015527_370_18156.log`。値変更1,865件の内訳は `apostles.js`基礎632、`statData.js`基礎633、同aside600で、それ以外の値経路は0。別途、利用者の未commit画像`Skill_{F,S,P}_Joanne.webp`を画像可用性生成器が検出し、`apostle-skill-image-data.js`のジョアン画像参照を更新した。これは今回の計算データ移行とは別の生成副作用で、削除・隠蔽しない。`stat-dashboard.html`の`statData.js`版hashも同期された。
- `node tools/test-aside-combat-power-v22-v29.js`は成功。v22原値のキャロット／ウイ、段階倍率、0・欠損・未解放、v29の8項丸め／防御合算／最終Truncate、スキル係数独立、A2以上1回、小数入力を検査。表示整数を使ったポーシャー例は参照実装どおり117709とし、ゲーム内記録117714と一致したとは報告しない。
- `node tools/test-aside-combat-power-native.js`は隔離Chromeで成功。管理画面APIと共通エンジンを同条件で比較し、キャロットA0～A3のアサイド内訳・戦闘力一致と内部小数／表示整数の分離を確認。`node tools/test-apostle-data-native.js`も成功し、79行と新しい基礎列を含む使徒データ画面を確認。`node tools/test-dps-audit-state.js`成功。生成内の画像・共有辞書・maintenance・生成データ検査は標準生成に含まれ成功済み。
- `tools/test-joanne-normal-damage-native.js`は現xlsxのジョアン性格が「裏面」、候補が「憂鬱・純粋」なのに、既存テストが「共鳴・冷静」を固定期待して性格選択待ちで停止。今回のアサイド／戦闘力式の成否には数えない。`tools/test-max-growth-state.js`は再生成に伴い固定fixtureの生成日時が古くなっており失敗。fixtureを手編集して通さない。`tools/test-public-release-config.js`は変更前から残る`app-cache.js`の資材版固定期待値不一致で停止（HEAD版テストにも同じ固定期待値がある）。全体`git diff --check`は生成器出力`apostles.js`の既存CRLF形式を末尾空白として報告するが、非生成の対象ファイルに限定した差分検査と構文検査は成功。
- 2026-09-26レビュー修正：管理snapshotに計算版2を記録し、敵使徒の育成値・学年上書きは整数化した最終値の差分更新をやめた。保存内訳の各加算源と、保存された育成設定から基礎・Rank・装備・好感度・アサイドを再計算して小数を保持する。キャロット本体Lv1／★3／学年1／A3の実UIで、アサイドLv1保存値からLv4へ上書きしたHPは旧処理5,010から直接計算と同じ5,011になった。Lv1へ戻し、再びLv4へ変更しても漂わない。学年変更も直接計算と照合した。
- 計算画面への直接アクセス時、旧版で全内訳と保存育成条件の整合が証明できる現在snapshotだけを現行式で読み取り用に再構築する。計算画面からは保存しないため、別タブの新しい育成状態を古い読み取り結果で上書きしない。旧planned値は独立した計画内訳を検証できない場合、旧board/research/rank-global値が非0の場合、または条件・内訳に欠損がある場合に停止し、管理画面で該当状態を読み込み再保存する。旧表示整数、旧アサイド、旧戦闘力を現行値として使わない。新形式の比較スロットは現在／予定それぞれの全内訳と計算版を保持する。旧圧縮スロットv1はボード・研究等の内訳が失われているため、現在の育成状態で代用しない。比較開始を止め、管理画面で該当スロットを読み込み再保存する案内を出す。旧保存計算は結果キャッシュを再評価できる場合だけ比較に使い、旧ピン留め比較基準は明示的な更新まで結果を表示しない。情報不足の旧snapshotは計算結果を「—」として再計算要求を表示し、通常計算・DPS入力へ入れない。保存データは削除しない。
- 隔離ブラウザで旧snapshotの直接読込・再読込、現在HP 4,362の再構築と、出所不明の旧planned値の停止、敵使徒Lv変更、旧v1比較スロット拒否、新v2スロット開始、旧保存計算の結果キャッシュを使わない比較再評価、旧ピン留め比較基準の再読込後の案内表示、情報不足snapshotの通常計算・DPS遮断および学年上書きでも遮断維持を確認。管理画面での再保存後は、現在と予定を区別する版2snapshotへ更新された。`tools/test-aside-combat-power-v22-v29.js`、`tools/test-aside-combat-power-native.js`、`tools/test-apostle-data-native.js`、`tools/test-announcements.js`は成功。既存の`tools/test-storage-behavior-baseline.js`はfixtureの`console.error`未実装、`tools/test-storage-baseline.js`は別案件の未登録`enemy-research.js`等で停止し、今回の成功には含めない。
- お知らせ`20260926-fix-aside-combat-power`はfixカテゴリのローカル追加。日付2026-09-26は利用者指定で確定済み。未公開であり、既存記事・重要案内・既読状態は変更しない。ゲーム内戦闘力の全条件完全一致、攻撃速度基礎以外の非戦闘時攻速補正、RenewaのAwaken実体と参照Hero UIDの関係は引き続き未確定。今回DPS速度処理には接続していない。

## 2026-09-26 再レビュー

- 旧snapshotの`stats.hp`などは表示整数であり、上書き時の内部値ではない。管理側・共通エンジンの直接計算に対し、敵使徒の実UIでアサイドLv1→4→1→4を行い、HP 4,362→5,011→4,362→5,011を確認。学年上書きも再構築経路を使う。旧保存の小数内訳は新計算で再生成し、戦闘力へは内部小数を渡す。現行snapshotは内訳・内部値・計算版を保持する。
- 旧値の移行条件を厳格化。全10項目の各内訳と率、育成のlevel/star/grade/rank/bond/asideRank/asideLevelを要求し、base/rankUp/equipment/bondが保存設定の再計算と一致することを検証する。nullは0と見なさない。旧boardBasic/boardAdvanced/research/rankGlobalが非0のときは出所を立証できないので自動移行しない。plannedはplanとboardDiffも要る。情報不足は現在値や0で補完しない。
- 敵使徒の個別上書きでA3境界をまたぐと、自身を含むA3全体補正率が変わり得る。単体の保存snapshotから他使徒も含む率は再構成できないため、旧率を持ち越した誤計算は行わず計算停止にする。管理側の全員再計算とは区別する。A3のままLvを変更する経路では率を1回だけ適用し、直接計算のHP 5,011と照合した。
- 隔離Chromeで、管理画面を先に開かない計算画面への旧保存直接読込、現在／予定の切替、旧v1比較スロットの停止案内、管理画面で同じスロットの育成・編成・性格・board planを読み込み再保存した後の比較復帰、新形式スロットの利用、再読込後の安定を確認。旧計算キャッシュの期待値は再利用しない。不足時は通常結果を`—`とし、DPS入力を停止する。成功結果が表示された後、別タブでsnapshotを欠損旧値に変更した際も結果・ピン留め期待値を消し、別タブの保存markerを維持した。
- この時点の保存基準テストは、`test-storage-behavior-baseline.js`がfixtureの`console.error`欠落でTypeError、`test-storage-baseline.js`が未追跡`enemy-research.js`と公開ファイル／台帳の不一致で停止していた。後続の公開前確認結果を下に記す。
- 今回の追加バックアップ台帳は`D:/Games/etc/trickcal/backups/aside-combat-rereview-20260926-1416/MANIFEST.tsv`。変更前ファイルの元パス・コピー先・SHA-256一致を記録。既存dirtyとxlsxは維持し、xlsx編集・再生成・commit・push・公開は行っていない。公開判断は基準テストの残件と、自動移行できない旧snapshotの案内を含めてレビュー待ち。

## 2026-09-26 公開前の追加確認（現時点の結論）

- `stat-engine.js`の敵使徒個別再計算は、表示整数を逆算せず保存育成設定・内訳から内部小数を再構築し、最後に表示値を丸める。キャロットA3・Lv1→4→1→4はHP 4,362→5,011→4,362→5,011。学年上書きも同じ再構築経路を使う。A3境界は対象使徒にA3全体ステータス効果が実在するときだけ停止し、該当効果がないfixtureでは再計算可能。率を推測・0補完しない。管理画面でアサイドを変更して保存し、計算画面でその保存設定を読み直すと、A3を下げる／上げる双方で通常計算とDPS入力が復帰することを隔離Chromeで確認した。
- 旧snapshotは内訳と育成条件が整合する現在値だけ自動再構築し、不一致・欠損・出所不明の予定値・旧圧縮比較スロットは停止する。旧比較スロットを管理画面で読み込む際、欠損育成項目を警告し、表示された既定値を元の保存値と見なさない。実ブラウザで欠損Lvを再設定してから再保存し、計算画面の比較に復帰した。編成の選択性格、A3、ボード計画は保持。育成条件が欠けている場合、単なる再保存だけで元の値を復元したとは主張しない。旧成功結果がある状態で別タブから不完全なsnapshotを保存すると、通常結果・比較期待値は消え、DPSも停止。計算画面から保存領域へ旧読込値を書き戻さず、別タブの新しいmarkerを保持した。
- 再レビューで実際の保存不具合を発見。全使徒のsnapshotを分割更新している最中にスロット保存すると、比較用圧縮内訳に選択使徒以外（ジョアン等）が欠落した。`saveCurrentStateToSlot`は保留中の更新作業を打ち切り、全員の現行snapshotを同期してからスロットを作る。隔離Chromeで79/79件とジョアンの完全内訳を確認し、再保存後の比較開始まで検証した。保存データ・育成条件を削除していない。
- `test-storage-behavior-baseline.js`のVM fixtureへ実`console`を渡し、障害注入で発生するerrorを隠さず、非同期のstorage-resumedイベントを待って件数を検証するよう修正。修正前の`console.error` TypeErrorは解消し、ライフサイクル・障害注入を含むテスト全体が終了コード0。`test-storage-baseline.js`はなおprojectInspectionで失敗する。具体的には未追跡のローカル試作`enemy-research.js`の未登録キー、追跡済み`shared-topbar.js`・`formation-share.html`の`trickcal_theme`参照、`service-worker.js`のcache prefix契約が台帳と一致しない。検査本体とこれらの対象は今回の保存版変更前から存在した。試作ファイルの削除・除外、台帳の機械的追認は行わない。この全体台帳検査は未通過であり、公開範囲の保存台帳整合性は未保証。公開手順へ進む前に、試作と公開対象の検査境界および公開ファイルの台帳修正を別途レビューする必要がある。
- 実施：`test-aside-combat-power-v22-v29.js`、`test-aside-combat-power-native.js`（隔離Chrome・実保存／再読込／別タブ）、`test-storage-behavior-baseline.js`、`test-announcements.js`成功。`test-storage-baseline.js`失敗。お知らせID`20260926-fix-aside-combat-power`は利用者確定日2026-09-26のfix記事1件で未公開。旧保存の不足条件は再設定して保存する旨を追記した。全条件のゲーム内戦闘力完全一致は主張しない。今回の編集前台帳は`D:/Games/etc/trickcal/backups/aside-combat-prepublish-20260926-151509/MANIFEST.csv`（dirtyを含む元パス・コピー先・SHA-256一致）に保存。xlsx編集・再生成・commit・push・公開は行っていない。

## 保存台帳と比較スロットの限定再確認（2026-09-26）

- Bの不一致を保存API・責務ごとに確認。`formation-share.html:12`の`trickcal_theme`読込と`shared-topbar.js:206,521`の同キー読書きは公開済み画面の共通テーマ初期化・切替で、`localStorage`例外を捕捉し、保存値がない場合はページの既定テーマへ進む。実装逸脱ではなく台帳の参照元・失敗経路の記載漏れなので`storage-inventory.json`へ追加した。キー削除はせず、ゲーム育成状態とは独立した表示設定として既存の移行扱いを維持する。
- `service-worker.js`の旧`CACHE_PREFIX`期待値は過去の静的契約。現行実装はscopeからnew/legacyのprofileを分けてCache Storage名を作り、自profileの所有cacheと明示した旧cache名だけを整理する。localStorageの設定保存ではなく、保存台帳のキーも増やさない。`storage-inspection.js`とBの静的検査をこの契約へ更新し、profile間cacheの混同を見逃さない条件を残した。
- 未追跡`enemy-research.js`は公開route/asset manifestに含まれずローカル試作。repo直下のJS/HTMLを探索する現行Bでは、存在すれば13箇所の`localStorage`読書きが対象になる。5キーは調査案件、編成版、選択案件、最近の敵名、試作専用テーマ。既存アプリの保存キーとは衝突しない。読込例外が試作起動・候補表示を妨げ得ること、案件・編成版の複数キー書込は原子的ではないことを確認した。試作実装は今回変更しない。台帳では公開`productionSources`と分離した任意の`localPrototypeSources`へ用途・API・失敗時挙動を記録し、ファイルがある場合に同じAST探索・未知キー/操作/領域検査をかける。公開対象だと偽った登録や、ファイル名除外はしていない。試作が同一Originで開かれた場合だけ、その固有キーのローカル保存へ影響する。
- 比較スロットは全使徒snapshotを同一イベントループで採取してから排他書込する。同期refreshの途中に例外を注入しても、保存済みスロットの生データは不変。スロットの`localStorage.setItem`失敗でも旧スロットは不変で成功表示は出ず、操作を再開できた。Navigator Lockを先に保持した状態で保存を開始すると「保存中」で再クリックを受け付けず、解放後のrevisionは1だけ進む。待機中に育成Lvを変更しても、保存された育成条件と圧縮計算内訳はクリック時点の同じデータに留まる。`saveState`による別のworkspace/live同期がスロット書込後に失敗した場合は、スロットは保存済みだが同期失敗と明示し、成功表示にはしない。異なる保存領域を原子的に戻す新基盤は導入しない。
- 隔離Chrome検証では実利用の保存領域を使わず、スロット書込失敗・refresh途中例外・排他待ち二重入力・待機中の育成変更を試した。既存dirty、xlsx、画像は保持。編集前の元パス・repo外退避先・SHA-256一致は`D:/Games/etc/trickcal/backups/aside-storage-gate-20260926-160425/MANIFEST.csv`。新旧計算式・生成データ・お知らせID/日付は今回変更していない。
- 保存系基準テストBは台帳分類後、旧`app-cache.js`の固定`?v=20260913a`と旧Service Worker cache版`20260913-storage-1`を期待する検査でさらに停止した。現在の`app-cache.js`はroute別に版なしの3つの保存共通層資材を列挙し、公開生成工程が版を付ける。Bを各routeの資材登録とprofile別cache版の接続を検査する形へ更新した。最終`node tools/test-storage-baseline.js`は成功（公開19キー、5チャネル、repo探索74アクセス。うち敵研究試作13アクセス／5キーを別分類で照合）。未登録キーを試作へ加える反例は引き続き失敗として検出する。公開対象だけの隔離fixtureも24登録元・61アクセス・不一致0だった。Bの通過は試作の複数キー保存を原子的にする保証ではなく、今回の公開範囲外の既存制約として残す。
- 保存系基準テストAも現行`stat-prototype.js`で再実行し、障害注入を含めて成功。対象コードの構文と対象限定`git diff --check`は成功。計算式や生成データは変更していないため、完了済みのv22/v29全計算検査・xlsx生成は繰り返さない。今回の保存台帳・比較スロット差分について公開を止める既知の問題はなく、対象差分のレビュー後に公開手順へ進める判断。ただし他の既存dirtyを無条件に公開対象へ混ぜない。
