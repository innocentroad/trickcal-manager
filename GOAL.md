# DPS発動経路・入力モード統合 Goal

## 今回のゴール（アクティブ）

DPSへ渡る発動条件を使徒や効果の個別名ではなく、発動条件binding単位で分類する。本人時計・共通時計で確定できる条件は`自動`、編成使徒のSP・CT・既知行動間隔から求める条件は`自動（推定）`を初期値とし、利用者が同じ設定カード内で周期・発生秒へ上書きできるようにする。

初期OFFは高学年が実際の発動元であるbindingだけに限定する。シールド破壊、被弾、HP閾値、撃破など単体DPS時計から発生を決められない条件は、真の外部条件として分離して残す。本体DPSとDPS試験版で分類、保存、実効イベント生成、表示を共有し、同じbindingを二重発火させない。

## 解消する現状の問題

1. `externalActionRequired`が、編成使徒の行動連動と真の外部条件の両方を表している。
2. 内部の`auto`が、画面では`外部入力`として表示される場合があり、利用者が自動を選択できない。
3. 編成行動の自動推定が効果別設定から離れた全体設定にあり、初期値がOFFになっている。
4. 高学年判定が理由文・表示名・効果対象を含む自由文を走査し、竜光剣やエレナ強化ドローンを初期OFFへ誤分類する。
5. 低学年と高学年を共有する効果を効果全体でOFFにするため、低学年側まで無効になる。
6. 現行テストは現在の分類と表示を期待値として固定しており、利用者要件との不一致を検出できない。

## 設計原則

- 論理効果、発動条件binding、発生時刻providerを分離する。
- 高学年の初期OFF判定には、構造化された発動元だけを使う。効果対象、説明、監査理由の文字列は使わない。
- 低学年と高学年を共有する条件は発動経路を分け、低学年は既定自動、高学年は既定OFFにする。
- 自動または推定可能なbindingは、使徒名に依存せず同じ規則で扱う。
- 手動周期・発生秒は同じbindingの自動providerを置換し、加算しない。
- 真の外部条件は自動と表示せず、入力がない間は外部入力待ちとする。
- ゲーム内未検証の推定値は`自動（推定）`と根拠を表示し、確定値と混同しない。

## 発動能力と初期方針

| 発動条件 | 発動能力 | 初期方針 |
| --- | --- | --- |
| 選択中本人の普通・強化・低学年、既知周期、計測済み効果 | 正確な自動 | 自動 |
| 編成使徒の普通攻撃、低学年、計算可能な強化攻撃 | 推定自動＋手動上書き | 自動（推定） |
| 高学年が発動元 | 自動または推定＋手動上書き | OFF |
| 自動処理済みの効果発生・状態・リソース遷移 | 自動連鎖 | 自動 |
| 内部イベントだが時刻だけ不足 | 周期または発生秒指定 | 入力待ち |
| シールド破壊・被弾・HP閾値・撃破・戦闘不能・対象変更 | 外部入力 | 外部入力待ち |
| 条件または処理内容を解釈できない | 未対応 | OFF・操作不可 |

効果対象が高学年であるだけのCT変更・補正は、高学年初期OFFの判定材料にしない。高学年行動が無効なら対象イベントが発生せず、結果へ自然に影響しない状態とする。

## 実装の順序

1. 発動経路監査: 全使徒・カード・編成候補をbinding単位で走査し、正確な自動、推定、外部、未対応、高学年を列挙する。既知の誤判定を回帰fixtureにする。
2. 共通分類の修正: 自由文依存を外し、発動元action、trigger、source lineage、外部条件種別から能力と初期方針を返すresolverへ統一する。
3. binding分割とprovider統合: 低学年／高学年共有条件を分割し、自動・推定・手動・外部の発生イベントを同じbinding resolverへ接続する。
4. 設定UIと保存移行: 各bindingカードで自動、自動（推定）、周期指定、発生秒指定、外部入力、OFFを能力に応じて表示し、選択時に必要な入力欄をその場で展開する。
5. 回帰・実画面・文書: 二重発火、更新後保持、比較fingerprint、本体／試験版一致、PC／スマホ、通常計算詳細、cache、仕様書を確認する。

## 完了条件

1. 全runtime効果と編成候補がbinding単位で`exact`、`estimated`、`external`、`unsupported`のいずれかに分類され、未分類は0件または理由付きで列挙される。
2. 本人行動・既知周期は既定`自動`、推定可能な編成普通・強化・低学年は既定`自動（推定）`として、追加操作なしで実効イベントへ反映される。
3. 高学年が発動元のbindingだけが初期OFFになる。効果対象・説明・監査理由に「高学年」があるだけでは初期OFFにならない。
4. 低学年と高学年を共有する効果は発動経路が分かれ、低学年側の自動を維持したまま高学年側だけをOFFにできる。
5. 自動・推定可能なbindingでは、そのカード内から周期または発生秒へ上書きできる。手動providerがある間は同じbindingの自動providerが停止し、二重発火しない。
6. シールド破壊・被弾・HP閾値・撃破などは外部条件として残り、自動と誤表示されない。入力したイベントは対応するbindingだけを一度発火させる。
7. 効果モードと発動方法を別々に保存し、既存の明示AUTO／FIXED／OFF、手動周期、外部イベントを失わない。旧`formationTimelineMode`は互換読み込みまたは一括操作に限定し、個別bindingの初期方針を上書きしない。
8. 本体DPSとDPS試験版が同じresolver、provider優先順位、保存形式、表示用語を使用する。
9. 竜光剣、エレナ強化ドローン、ロレッタ低学年由来効果、キャロット低学年、低高共有効果、キャロットA2シールド破壊を代表fixtureとして期待状態を固定する。
10. 対象テスト、構文検査、実データ監査、`git diff --check`、PC／スマホ実画面確認を完了し、通常計算詳細と既存DPS比較を壊さない。datasheetは直接変更しない。

## 保存移行の方針

- 新しいbinding設定がない場合は、このGoalの初期方針から決定する。旧全体設定の既定OFFを、そのまま全bindingの明示OFFとして移行しない。
- 既存の明示的な効果別`auto`／`fixed`／`off`は維持する。
- 既存の周期イベントは、一致するcandidate IDまたはbinding keyがあれば手動providerとして取り込む。
- シールド破壊など既存の外部イベントはそのまま保持する。
- 移行後も旧形式を読み込める期間を設け、保存し直すまでは破棄しない。

## 非目標

- 敵AI、被弾、シールド残量、撃破時刻を新たに自動シミュレーションすること。
- 編成使徒本人のダメージを合算するフル編成DPS。
- 編成全員の厳密な行動待ち、モーション競合、SP授受を完成させること。
- ゲーム内未検証の周期、処理順、丸めを確定扱いにすること。
- 今回の分類修正と無関係な通常計算の変更、datasheetの直接編集。

## 制約

- 未コミット変更をすべて維持する。
- `git reset --hard`、`git checkout`による一括復元を行わない。
- 個別使徒の例外追加で完了扱いにせず、共通分類と代表fixtureで解決する。
- 計算結果を変えるスライスでは、実装前後の対象bindingと発火回数を比較する。

## 1スライスの定義

1スライス = 開始前に作業内容と効く完了条件を明示 → 実装または監査 → 対象検証 → 達成度・根拠・残り見積もりを`STATUS.md`へ反映、の1ループとする。

3スライス連続で達成度がほぼ増えない場合は、局所修正を続けず、分類モデル・スコープ・完了条件を再レビューして再計画案を提示する。

## 開始時点

2026-09-03の再監査で、旧後続Goalの100%判定後にも、竜光剣の高学年誤判定、編成低学年の自動選択不在、低高共有効果の全体OFFが残っていることを確認した。旧Goalの実装と検証記録は履歴として維持し、本Goalは達成度0%から開始する。
