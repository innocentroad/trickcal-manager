# DPS実装 Goal

## 今回のゴール

Trickcal Manager 本体の DPS 残実装を、既存の通常ダメージ計算・DPS試験版・本体下バー詳細・DPS計算フロートと整合する形で、優先順位を付けて段階的に完成させる。

ゲーム内検証が必要な数値・発動時刻・処理順・ランダム量は、検証済みの事実と混同しない暫定処理として実装する。暫定であることをランタイム結果、詳細表示、仕様書または検証待ち台帳から追跡できる状態にする。

## 実装の順序

1. 共通戦闘時計: 行動開始・終了、モーション硬直、ヒット、攻撃速度、SP、CTを一つの時系列で扱う。
2. 戦闘中の変化: 状態・バフ・デバフ・固有状態・リソースの付与、更新、終了、スタック、追加効果を同じ時系列へ接続する。
3. 条件発動: 低学年/高学年の使用・命中・終了、生成物、効果連鎖、回数条件、外部イベントを、再現可能なものから実装する。敵行動・被弾・撃破など時刻を自動決定できないものは入力または暫定扱いにする。
4. 本体表示: 下バー詳細の行動別適用効果、単一seed行動タイムライン、ダメージ推移、モーション情報、外部イベント設定を確認できるようにする。通常計算の詳細シートは別責務として維持する。
5. 比較・品質: 基準/変更値と変化率、設定保存、再計算中表示、狭い幅の表示、構文・回帰テスト、仕様書・台帳・キャッシュ整合、必要な実画面確認を完了する。

## 完了条件

1. DPSシミュレーターが、行動・モーション・ヒット・攻撃速度・SP・CT・状態・リソース・追加効果を同じ戦闘時計で処理し、同じイベントを二重計上しない。
2. 行動別適用効果、単一seed行動タイムライン、外部イベント、ダメージ推移、通常攻撃間隔、モーション硬直を本体DPS下バー詳細で確認・操作できる。
3. DPS計算設定・外部イベント・比較表示・再計算中表示・レスポンシブ表示が整合し、通常計算の詳細シートを壊さない。
4. 未検証の発動条件、状態遷移、処理順、丸め、ランダム量、敵側イベントは、暫定処理・表示上の注記・検証待ち記録の三者が追跡可能になっている。
5. 既存の通常計算と直近のDPS回帰を壊さず、対象テスト、構文検査、`git diff --check`、必要な実画面確認を完了する。
6. datasheetを直接編集せず、未実装データの修正が必要な場合は修正内容を台帳へ明示し、未コミット変更を維持したまま引き継ぎ可能にする。

## 暫定処理のルール

- 既存の生成データ、skillmotion、明示された既存モデルを優先し、検証状態が分かる品質情報を引き継ぐ。
- 発生時刻を決められる既存の行動・効果は共通時計へ接続する。決められないイベントは、手動入力、周期の予測初期値、または発動無効のいずれかを理由付きで選ぶ。
- 範囲値・ランダム値は単一seedで再現可能な暫定値として扱い、実測値が得られるまで確定値と表示しない。
- 暫定処理を追加したら、対象ID、仮定、影響範囲、ゲーム内で確認すべき内容を仕様書または台帳へ記録する。

## 非目標

- ゲーム内検証そのものを完了したとみなすこと、未検証値を実測済みとして確定すること。
- 既存データに根拠のない敵全体の行動・被弾・撃破タイムラインを自動推測すること。
- 今回のDPS作業と無関係な通常計算の仕様変更、またはdatasheetの直接編集。

## 制約

- 未コミット変更を維持する。
- `git reset --hard`、`git checkout`による一括復元を行わない。
- 実装は既存モデルを優先し、共通意味論を変更する場合は仕様書へ記録する。

## 1スライスの定義

1スライス = 次の作業内容と効く完了条件を明示 → 実装または台帳整理 → 対象検証 → 達成度・根拠・残り見積もりを `STATUS.md` に反映、の1ループ。
